#include <cg/operations/point_in_convex_polygon.h>
using namespace cg;

int main{
	//point_2 a(0, 0), b(1, 0), c(2, 1), d(2, 2), e(1, 3), f(0, 3), g(-1, 2), h(-1, 1), x(-0.0001, 0);
    //std::cout << convex_polygon_point(contour_2({a, b ,c, d, e, f, g, h}), x) << std::endl;
	return 0;
}